# This file is generated by the SLSA GitHub Generator.

name: SLSA3 Publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Upgrade to latest SLSA GitHub Generator
        uses: slsa-framework/slsa-github-generator@latest

      - name: Generate SLSA metadata
        id: slsa
        run: |
          echo "::set-output name=digests::$(cat path/to/your/hashes/file)"

      - name: Upload SLSA metadata
        run: |
          # your upload command here
